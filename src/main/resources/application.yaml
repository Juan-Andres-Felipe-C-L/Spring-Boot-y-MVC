spring:
  application:
    name: ParkingKR

  datasource:
    url: jdbc:mysql://localhost:3306/ParkingKR
    username: root
    password: "0000"
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        "[format_sql]": true

  thymeleaf:
    cache: false

server:
  port: 8080